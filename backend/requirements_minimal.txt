# Atmosphere Analyzer Backend - Minimal Working Requirements
# Essential dependencies that are proven to work with the NASA hackathon project

# === CORE FRAMEWORK ===
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# === HTTP & API CLIENTS ===
httpx>=0.25.0
requests>=2.31.0
aiohttp>=3.9.0
urllib3>=2.1.0

# === DATABASE & SUPABASE ===
supabase>=2.3.0
postgrest>=0.16.0
realtime>=1.0.0
gotrue>=2.12.4
storage3>=0.7.0

# === ESSENTIAL DATA PROCESSING ===
pytz>=2023.3
python-dateutil>=2.8.2

# === GEOSPATIAL ===
geopy>=2.4.0

# === XML PROCESSING (for NASA APIs) ===
lxml>=5.0.0

# === ENVIRONMENTAL VARIABLES ===
python-dotenv>=1.0.0

# === ASYNC SUPPORT ===
aiofiles>=23.2.0

# === AUTHENTICATION & SECURITY ===
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
python-multipart>=0.0.6
cryptography>=41.0.0

# === LOGGING ===
loguru>=0.7.0

# === CACHING ===
# Removed Redis - using in-memory caching instead

# === TESTING ===
pytest>=7.4.0
pytest-asyncio>=0.21.0

# Note: Many scientific packages (pandas, numpy, scipy, sklearn, matplotlib, etc.)
# are already installed in the Python environment and don't need to be reinstalled.
# The NASA TEMPO service works with the existing environment.